# BunkerOS Power Management Configuration
# This configures systemd-logind to handle automatic suspend

[Login]
# Suspend after 10 minutes of idle time (600 seconds)
# This works in conjunction with swayidle screensaver (5 min)
# Result: screensaver at 5 min, suspend at 10 min
IdleAction=suspend
IdleActionSec=10min

# Handle lid close events (for laptops)
HandleLidSwitch=suspend
HandleLidSwitchExternalPower=suspend

# Handle power button
HandlePowerKey=poweroff
