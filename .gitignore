*.backup
*.swp
*.swo
*~
.DS_Store

# Lite-xl session files (user-specific)
lite-xl/session.lua
lite-xl/ws/

# Auto-scaling modifies theme files - exclude to prevent conflicts
# Users should have these in ~/.config instead
themes/*/foot.ini
themes/*/waybar-style.css
themes/*/wofi-style.css

# Keep only the template versions (add .template extension)
!themes/*/*.template

# Web app user data (stored in ~/.local/share/sway-webapp/)
webapp/icons/
webapp/data/

# Ignore current theme preference (user-specific)
.current-theme

# User-specific BunkerOS config
.config/bunkeros/

# Theme-applied config files (user's active theme)
# These are generated from .default versions and modified by theme-switcher.sh
# Git tracks the .default versions, not the active user configs
bashrc
dircolors
foot/foot.ini
mako/config
sway/config
swayosd/style.css
waybar/style.css
wofi/style.css
btop/themes/active.theme
